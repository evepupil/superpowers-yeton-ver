interface:
  display_name: "QA 接口测试设计"
  short_description: "仅在材料充分且用户确认时设计接口测试点和自动化草案"
  default_prompt: "Use $qa-api-test-design only when API testing is explicitly requested and repository or API documentation is available."
policy:
  allow_implicit_invocation: true
